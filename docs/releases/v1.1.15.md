# DI-Viewer v1.1.15

本次版本主要修复 macOS 安装体验与发布产物策略。

## 修复与调整

- 修复 `dmg` 安装后应用被系统提示“已损坏”的高频问题：
  - 在 CI 中对 macOS `.app` 进行 ad-hoc 签名后再封装 `dmg`（不引入 Developer ID / notarization）。
- 调整 Release 资产：
  - 不再将 `SHA256SUMS` 上传到 GitHub Release 资产列表。

## 产物

- Windows x64：`exe`、`msi`
- macOS arm64：`dmg`

