## Windows browser shell fix

- Fixed missing sidebar controls in Windows builds by packaging the shared browser shell assets with the Tauri bundle.
- Removed injected page padding that could create an unwanted right-side scrollbar in the embedded webpage.
- Re-runs browser shell injection after the browser window is created and after page loads, improving first-launch reliability on WebView2.

