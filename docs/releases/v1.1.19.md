## Windows sidebar and icon fix

- Injects the browser shell at document start so the sidebar is mounted reliably on Windows WebView2.
- Grants the embedded browser window an explicit remote capability for the controlled bridge commands used by DI-Viewer.
- Regenerates Windows icon assets, including a multi-size `.ico`, so the program and installer use the current logo.

